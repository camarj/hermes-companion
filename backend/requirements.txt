fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
openai>=1.30.0
httpx>=0.27.0
websockets>=12.0
pyyaml>=6.0
pillow>=10.0.0
numpy>=1.26.0
pypdf>=4.0.0
# face_recognition is optional — vision works without it (just no name lookup).
# Install separately via ./install_face_recognition.sh — it pulls dlib which
# needs cmake + a C++ toolchain.
